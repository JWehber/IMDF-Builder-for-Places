# Copy this file to `.env` — docker compose reads it automatically.

# Host port to expose the app on. Change this if port 3000 is already in use.
HOST_PORT=3000

# Container timezone (any IANA name, e.g. Australia/Sydney, America/New_York).
TZ=UTC
